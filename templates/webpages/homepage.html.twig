{# Se hereda la plantilla principal. #}
{% extends 'base.html.twig' %}

{# Sobreescribimos el bloque del título del fichero base, añadimos también contenido propio
   de esta forma se puede observar como obtenemos el título de la plantilla y le añadimos lo
   que deseemos. Para ello utilizamos la funcion parent() dentro de llaves.
#}
{% block title %}{{ parent() }} | Bienvenido{% endblock %}

{#  Sobreescribimos la plantilla en el bloque body.
    Esto substitye el body generadn en este fichero twig por el existente en la plantilla base.
#}
{% block body %}

{# Se interpola el título y la descripción mediante sistema de llaves. #}
<h1>{{ titulo }}</h1>
<p>{{ descripcion }}</p>

<h2>Lenguajes de programación más utilizados en Aug 2022:</h2>

{# Se crea un listado desordenado mediante UL. #}
<ul>
    {# Se crea un bucle FOR para recorrer el array enviado en la función homepage. #}
    {% for lenguajes in lenguajesProgram %}
    <li>
        {# Se imprimen los diferentes lenguajes de programación.#}
        {{ lenguajes.idioma | upper }} - {{ lenguajes.uso }}
    </li>
    {# Fin del bucle FOR. #}
    {% endfor %}
</ul>

{# Fin del cuerpo. #}
{% endblock %}